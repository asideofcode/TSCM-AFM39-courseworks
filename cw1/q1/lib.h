#include <stdbool.h>

// Declare function signatures to avoid implicit declaration
bool isLeapYear(int year);
int getDaysInMonth(int year, int month);
int calculateDaysFromStart(int year, int month, int day);
int calculateDayOfWeek(int year, int month, int day);
int howManyDays();
